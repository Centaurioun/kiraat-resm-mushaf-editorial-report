# Fourth Wave — Short Worker Startup Prompts

These prompts intentionally contain only routing and scope language. The authoritative instructions are in each task file.

## Worker 1
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W01.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 2
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W02.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 3
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W03.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 4
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W04.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 5
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W05.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 6
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W06.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 7
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W07.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 8
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W08.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 9
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W09.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.

## Worker 10
Use `Centaurioun/kiraat-resm-mushaf-editorial-report`. On branch `orchestration/f4f5-parallel-v1`, read `parallel/tasks/fourth/F4-W10.md` and every binding contract it references. Execute exactly and only that task, on the worker branch specified there. Do not perform any other worker's items or any Fifth work.
